{
  "name": "stackoverflow-parcel-bootstrap",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "start": "parcel src/index.html --no-autoinstall --open",
    "build": "parcel build src/index.html --no-autoinstall",
    "clean": "rimraf dist .parcel-cache"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel": "^2.0.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "bootstrap": "^5.1.3"
  }
}

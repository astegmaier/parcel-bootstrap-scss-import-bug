{
  "name": "parcel-bootstrap-scss-import-bug",
  "version": "1.0.0",
  "description": "A repro for a bug in parcel while importing scss files from bootstrap.",
  "scripts": {
    "start": "parcel src/index.html --open",
    "build": "parcel build src/index.html",
    "clean": "rimraf dist .parcel-cache"
  },
  "author": "Andrew Stegmaier",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.0.0",
    "parcel": "^2.0.0",
    "rimraf": "^3.0.2"
  },
  "dependencies": {
    "bootstrap": "^5.1.3"
  }
}
